function indexRoute(req, res){	var hbh = require('hbHelpers.js');	var agent = ds.Agent.find("domain == :1", req.host);	if(agent == null){		var context = {			title: "The Company Page",			intro: "This is the company website, not an individual agent",			listings: ds.Listing.all().toArray()		};	} else {		var context = {			title: agent.fullName,			into: agent.webIntro,			listings: ds.Listing.query("agent.ID == :1", agent.ID).toArray()		};	}	res.body = hbh.process('companyTemplate.hbs', context);	res.headers.CONTENT_TYPE =  "text/html";	return res;}function showingRequestRoute(req, res){	debugger;	//look in req.parts[0] for values	}