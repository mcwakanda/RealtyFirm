/*	The helloWorld() function can be executed from any of your project's server-side JavaScript file using the require() method as follows:	var result = require('hbHelpers').helloWorld();	For more information, refer to http://doc.wakanda.org/Wakanda Studio0.Beta/help/Title/en/page3355.html*/var templateFolder = getFolder("path") + "templates/"; //uses project/templates as defaultexports.setTemplateFolder = function setTemplateFolder (relativePath) {	//sets the path according to the project folder based on a relative path	return getFolder("path") + relativePath;};exports.process = function(templateName, data){	var result = Handlebars.compile(File(templateFolder + templateName).toString())(data);	return result;};